# ============================================
# Invoice Processor - Production dependencies
# ============================================
# WAŻNE: Instaluj stopniowo zgodnie z planem.
# W dniu 1 NIE potrzebujesz wszystkiego!
#
# Jak instalować: pip install -r requirements.txt

# --- Tydzień 1 Dzień 1: FastAPI podstawy ---
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# --- Phase 1: PDF text extraction (primary, text-PDFs) ---
pdfplumber==0.11.4

# --- Tydzień 1 Dzień 2: OCR + OpenAI ---
pytesseract==0.3.13
pdf2image==1.17.0
Pillow==11.0.0
openai==1.51.0

# --- Phase 3: Database (async SQLAlchemy 2.0, no Alembic) ---
sqlalchemy==2.0.35
asyncpg==0.30.0        # async Postgres driver (replaces psycopg2-binary)
aiosqlite==0.20.0      # async SQLite for in-memory test DB
greenlet==3.4.0        # required by SQLAlchemy async on CPython 3.13 Windows
pydantic-settings==2.5.2
python-dotenv==1.0.1

# --- Tydzień 2 Dzień 8-9: Redis + Queue ---
redis==5.1.1
rq==2.0.0

# --- Tydzień 2 Dzień 10: Qdrant + Embeddings ---
qdrant-client==1.11.3
sentence-transformers==3.1.1

# --- Tydzień 2 Dzień 11: HuggingFace Transformers ---
transformers==4.45.2
torch==2.4.1

# --- Utilities ---
httpx==0.27.2
tenacity==9.0.0  # retry logic
